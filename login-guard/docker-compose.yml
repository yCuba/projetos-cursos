version: '3.8'
services:
  db:
    image: postgres:15
    container_name: login-guard-db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: loginguard
    ports:
      - "5432:5432"